<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.programadorjm.service">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK"/><!--Pemissão para veficiar estado da tela, JOBINTENTSERVICE-->

    <application
        android:allowBackup="true"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Service">
        <activity android:name=".JobIntentServiceActivity" />
        <activity android:name=".BackgroundServiceActivity" />
        <activity android:name=".BoundActivityBinder" />
        <activity android:name=".ForegroundActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Declarando os servicos -->
        <service android:name=".ForegroundService" />
        <service android:name=".BoundServiceIbinder" />
        <service android:name=".BackgroundService" />
        <service android:name=".ServiceJobIntent" android:permission="android.permission.BIND_JOB_SERVICE"/>
    </application>

</manifest>